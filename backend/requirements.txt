fastapi==0.139.0
uvicorn[standard]==0.51.0
sqlmodel==0.0.39
pydantic==2.13.4
pydantic-settings==2.14.2
PyJWT==2.13.0  # replaced python-jose: its ecdsa dependency is flagged (PYSEC-2026-1325) with no fix upstream; this HS256-only app never used ECDSA
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 is incompatible with bcrypt>=4.1's strict 72-byte check
python-multipart==0.0.32
email-validator
langid==1.1.6
pytest==9.1.1
httpx==0.28.1

# For the real transformer-based multilingual translation model
# (required once USE_MOCK_TRANSLATION=false is set in .env):
# transformers>=4.46
# torch>=2.5
# sentencepiece>=0.2
pytest-cov==7.1.0
alembic==1.18.5
